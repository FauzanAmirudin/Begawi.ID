export default {
    plugins: {
        // Semua CSS files sekarang diproses oleh @tailwindcss/vite (Tailwind v4)
        // PostCSS hanya menjalankan autoprefixer untuk vendor prefixes
        autoprefixer: {},
    },
};
